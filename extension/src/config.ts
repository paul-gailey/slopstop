// The Modal/inference endpoint URL is set by the user in the popup's Settings
// panel and persisted to chrome.storage.local under this key. Background and
// popup both read it from there — no compiled-in URL.
export const STORAGE_KEY_BACKEND_URL = "backend_url";
